"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _typeof2 = _interopRequireDefault(require("@babel/runtime/helpers/typeof"));

var _shouldPolyfill = require("@formatjs/intl-relativetimeformat/should-polyfill");

/* eslint-disable global-require, import/no-named-as-default */

/**
   * Use try-catch to check if Intl is provided by the browser. In some instances checking Intl will throw an
   * error and crash the page with little information.
   *
   * Reference: https://github.com/cerner/terra-core/issues/2820
   */
var hasIntl;

try {
  hasIntl = (typeof Intl === "undefined" ? "undefined" : (0, _typeof2.default)(Intl)) === 'object' && typeof Intl.DateTimeFormat === 'function' && typeof Intl.NumberFormat === 'function' && typeof Intl.PluralRules === 'function';
} catch (error) {
  hasIntl = false;
}

if (!hasIntl) {
  require('intl');
}

if ((0, _shouldPolyfill.shouldPolyfill)()) {
  require('@formatjs/intl-relativetimeformat/polyfill');
}
/* eslint-enable global-require */