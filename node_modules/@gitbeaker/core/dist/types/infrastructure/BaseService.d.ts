import { RequesterType } from '@gitbeaker/requester-utils';
export interface BaseServiceOptions {
    oauthToken?: string;
    token?: string;
    jobToken?: string;
    host?: string;
    url?: string;
    version?: 3 | 4;
    rejectUnauthorized?: boolean;
    camelize?: boolean;
    requester?: RequesterType;
    requestTimeout?: number;
    profileToken?: string;
    sudo?: string | number;
    profileMode?: 'execution' | 'memory';
}
export declare class BaseService {
    readonly url: string;
    readonly requester: RequesterType;
    readonly requestTimeout: number;
    readonly headers: {
        [header: string]: string;
    };
    readonly camelize: boolean;
    readonly rejectUnauthorized: boolean;
    constructor({ token, jobToken, oauthToken, sudo, profileToken, requester, profileMode, host, url, version, camelize, rejectUnauthorized, requestTimeout, }?: BaseServiceOptions);
}
