import { NormalizedOptions } from "./options";
import { NodeInfo, UrlMatch } from "./types";
/**
 * Returns a node's URL(s)
 */
export declare function findUrls(info: NodeInfo, options: NormalizedOptions): IterableIterator<UrlMatch>;
