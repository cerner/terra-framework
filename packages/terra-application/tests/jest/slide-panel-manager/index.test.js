import SlidePanelManager from '../../../src/slide-panel-manager';

describe('slide-panel-manager/index', () => {
  it('should export SlidePanelManager', () => {
    expect(SlidePanelManager).toBeDefined();
  });
});
