import CustomSlider from './CustomSlider?dev-site-example';

# Custom Slider

### Description

This example demonstrates a [slider](/components/cerner-terra-framework-docs/slider/about) with a custom label and minimum and maximum labels. The minimumLabel and maximumLabel properties are used to set custom labels. These labels are also used to construct the hidden text that assists screen reader users. 

<CustomSlider />