import RowSelection from './RowSelection?dev-site-example';

# Row Selection

### Description

In addition to adding selection behavior to individual cells within the grid, the **onCellSelect** callback can provide logic to highlight a row of cells when selecting a row header cell, as demonstrated in the following example.

<RowSelection title= 'Flowsheet Data Grid with Single Row Selection' />
