import CompactInteractiveListSetWidth from './CompactInteractiveListSetWidth?dev-site-example';

The Compact Interactive List `width` prop set to be `800px`. It doesn't let the list to change it's width.

If the container width is less than 800px, a scroll will be added.


<CompactInteractiveListSetWidth />